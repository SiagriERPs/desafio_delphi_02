unit Testunit_fProduto;
{

  Delphi DUnit Test Case
  ----------------------
  This unit contains a skeleton test case class generated by the Test Case Wizard.
  Modify the generated code to correctly setup and call the methods from the unit 
  being tested.

}

interface

uses
  TestFramework, Vcl.Buttons, FireDAC.DApt.Intf, FireDAC.DatS, FireDAC.Stan.Param,
  System.ImageList, System.Classes, Vcl.ExtCtrls, biblioteca, unit_fPadrao,
  Vcl.StdCtrls, FireDAC.Stan.Intf, Vcl.Grids, ProdutoVO, Vcl.ComCtrls,
  FireDAC.Stan.Error, Vcl.Graphics, FireDAC.Phys.Intf, FireDAC.Comp.Client,
  Winapi.Windows, System.Variants, Winapi.Messages, Vcl.Dialogs, Vcl.ImgList, Vcl.Forms,
  System.SysUtils, Data.DB, Vcl.Controls, unit_fProduto, FireDAC.Stan.Option,
  FireDAC.Comp.DataSet, Vcl.DBGrids, Vcl.Imaging.pngimage, ProdutoCCliente;

type
  // Test methods for class TfProduto

  TestTfProduto = class(TTestCase)
  strict private
    FfProduto: TfProduto;
  public
    procedure SetUp; override;
    procedure TearDown; override;
  published
    procedure TestvalidacaoNome;
    procedure TestvalidacaoPreco;
  end;

implementation

procedure TestTfProduto.SetUp;
begin
  FfProduto := TfProduto.Create(nil);
end;

procedure TestTfProduto.TearDown;
begin
  FfProduto.Free;
  FfProduto := nil;
end;

procedure TestTfProduto.TestvalidacaoNome;
var
  ReturnValue: Boolean;
  sNome: string;
begin
  // TODO: Setup method call parameters
  sNome := 'Veneno';
  ReturnValue := FfProduto.validacaoNome(sNome);
  // TODO: Validate method results
  CheckTrue(ReturnValue);
end;

procedure TestTfProduto.TestvalidacaoPreco;
var
  ReturnValue: Boolean;
  cValor: Currency;
begin
  // TODO: Setup method call parameters
  cValor := 1.0;
  ReturnValue := FfProduto.validacaoPreco(cValor);
  CheckTrue(ReturnValue);
  // TODO: Validate method results
end;

initialization
  // Register any test cases with the test runner
  RegisterTest(TestTfProduto.Suite);
end.

